# == Native Modules

# == Installed Modules
from setuptools import setup, find_packages
# == Project Modules


setup(
    name='mEdit',
    version='0.1',
    description='',
    author='Interventional Genomics Unit',
    author_email='',
    packages=find_packages(),
)